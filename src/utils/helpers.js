/**
 * Utility helper functions for the ShopiKart My Orders Portal
 */

export const formatPrice = (price) => {
  return price;
};

export const formatDate = (dateString) => {
  return dateString;
};
